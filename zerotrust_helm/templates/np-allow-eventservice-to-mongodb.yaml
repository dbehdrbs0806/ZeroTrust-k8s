apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-eventservice-to-mongodb
  namespace: {{ .Release.Namespace }}
spec:
  podSelector:
    matchLabels:
      app: {{ .Values.mongodb.name }}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: {{ .Values.eventService.name }}
      ports:
        - protocol: TCP
          port: {{ .Values.mongodb.service.port }}